const getUser = () =>{
    const user ={
        name: 'Dorian',
        id:id
    }
    console.log(user);

}
getUser(1, ()=>{
getUser(1, (err, user)=>)
    if(err) return console.log(err)
    console.log('User name is');
})